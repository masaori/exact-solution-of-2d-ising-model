# Task: 026_claim_A_thetaの対角化_固有値と固有ベクトル.typ の修正

ステータス：未完了

## 対象ファイル
`parts/008_T_V1_hatZとhatZ_hatYの関係/026_claim_A_thetaの対角化_固有値と固有ベクトル.typ`

## 変更概要
`arg^((-pi, pi])` を `arg^([0, 2pi))` に変更する。
$A(\theta)$ の対角化に関する claim を修正する。3箇所の変更が必要。

## 変更のポイント

### 記号の変更
- `arg^((-pi, pi])` → `arg^([0, 2pi))`

### 具体的な変更箇所
1. `arg^((-pi, pi])(-1)_(CC)` — $-1$ の偏角
   - 旧: `arg^((-pi, pi])(-1_(CC)) = pi`
   - 新: `arg^([0, 2pi))(-1_(CC)) = pi`（piは`[0, 2pi)`の範囲内なので値は変わらない）

2. `arg^((-pi, pi])((...)` — 具体的な複素数の偏角
   - 新しい区間での値を再計算

3. `because #ref(<inverse_of_sqrt_cc>) "かつ" arg^((-pi, pi])(-1_(CC)) = pi`
   - 042の結果の引用部分を更新

### 参照先の変更
- `#ref(<condition_of_commutativity_of_sqrt_and_product>)` → 039 の結果を使っている
- `#ref(<inverse_of_sqrt_cc>)` → 042 の結果を使っている
- どちらも新しい `[0, 2pi)` 版の結果に合わせること

## 変更の参考例
`parts/000_計算公式/031_claim_複素数の積のarg.typ` で既に同様の変更が行われている。
変更前後を比較して、記法変更のパターンを理解すること。

## 注意事項
- 1行も省略しないこと。意図的に冗長に全ての計算ステップを明示している。
- 証明の全行を書き直すこと。途中のステップを省略してはならない。
- 039 と 042 の修正が先に終わっていること。
- $\arg^{[0,2\pi)}(-1) = \pi$ は変わらないが、引用している条件の形が039, 042の修正に伴い変わる可能性がある。
