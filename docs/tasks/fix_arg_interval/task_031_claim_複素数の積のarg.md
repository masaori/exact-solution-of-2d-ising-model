# Task: 031_claim_複素数の積のarg.typ の修正（コメントアウト削除）

ステータス：未完了

## 対象ファイル
`parts/000_計算公式/031_claim_複素数の積のarg.typ`

## 変更概要
このファイルは既に `[0, 2pi)` 版に変更済み。旧版（`(-pi, pi]` 版）がコメントアウトして残されているので、コメントアウト部分を削除する。

## 変更のポイント
- ファイル冒頭の `//` で始まるコメントアウト行（旧 `(-pi, pi]` 版の claim と proof）を全て削除する。
- 新しい `[0, 2pi)` 版の claim と proof はそのまま残す。

## 注意事項
- コメントアウト部分のみ削除し、現在有効なコード（`[0, 2pi)` 版）には一切手を加えないこと。
