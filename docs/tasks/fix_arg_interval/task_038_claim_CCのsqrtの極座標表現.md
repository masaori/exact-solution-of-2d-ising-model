# Task: 038_claim_CCのsqrtの極座標表現による展開.typ の修正

ステータス：未完了

## 対象ファイル
`parts/000_計算公式/038_claim_CCのsqrtの極座標表現による展開.typ`

## 変更概要
`s_((-pi, pi])` を `s_([0, 2pi))` に変更する。
sqrtの極座標展開の証明中に `s_((-pi, pi])` が1箇所使われている。

## 変更のポイント

### 記号の変更
- `s_((-pi, pi])` → `s_([0, 2pi))`

### 式の変更
旧:
```typst
1/2 dot.op s_((-pi, pi])([theta]_(~_(angle)))
```

新:
```typst
1/2 dot.op s_([0, 2pi))([theta]_(~_(angle)))
```

## 変更の参考例
`parts/000_計算公式/031_claim_複素数の積のarg.typ` で既に同様の変更が行われている。
変更前後を比較して、記法変更のパターンを理解すること。

## 注意事項
- 1行も省略しないこと。意図的に冗長に全ての計算ステップを明示している。
- 証明の全行を書き直すこと。途中のステップを省略してはならない。
