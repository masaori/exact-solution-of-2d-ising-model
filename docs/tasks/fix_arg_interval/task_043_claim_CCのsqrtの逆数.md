# Task: 043_claim_CCのsqrtの逆数とremark.typ の修正

ステータス：未完了

## 対象ファイル
`parts/000_計算公式/043_claim_CCのsqrtの逆数とremark.typ`

## 変更概要
`arg^((-pi, pi])` を `arg^([0, 2pi))` に変更する。
$1/\sqrt{z}$ に関する claim と remark を修正する。

## 変更のポイント

### 記号の変更
- `arg^((-pi, pi])` → `arg^([0, 2pi))`

### 不等式の変更
旧: `-pi < ... < pi` / `arg(z) = pi` 系
新: `0 <= ... < 2pi` / `arg(z) = 0` 系（または新しい分岐条件）

**注意: 等号のつく場所が逆になる。**

### 主張の分岐が変わる
旧 `(-pi, pi]` 版:
```
sqrt(1/z)      (-pi < arg(z) < pi)
-(sqrt(1/z))   (arg(z) = pi)
```

新 `[0, 2pi)` 版では:
- 042（逆数のsqrt）の結果に基づいて分岐が変わる
- `#ref(<inverse_of_sqrt_cc>)` を使っている

### 証明中の `#ref` について
- `#ref(<inverse_of_sqrt_cc>)` → 042 を参照している
- 042 の修正結果に合わせて、引用している条件を更新すること

## 変更の参考例
`parts/000_計算公式/031_claim_複素数の積のarg.typ` で既に同様の変更が行われている。
このファイルでは:
- 旧版がコメントアウトされている
- 新版が `[0, 2pi)` の記法で記述されている

変更前後を比較して、以下を理解すること:
1. 記号 `(-pi, pi]` → `[0, 2pi)` の機械的な置換部分
2. 不等式の向きが逆になる（`<` と `<=` の入れ替え）
3. 分岐条件の再導出が必要な部分

## 注意事項
- 1行も省略しないこと。意図的に冗長に全ての計算ステップを明示している。
- 証明の全行を書き直すこと。途中のステップを省略してはならない。
- 042（逆数のsqrt）の修正結果に依存するので、042の修正が先に終わっていること。
