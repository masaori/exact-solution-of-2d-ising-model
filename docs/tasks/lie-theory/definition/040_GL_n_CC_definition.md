# GL(n,CC)の定義整備

## 概要

一般線型群 GL(n,CC) とその群構造の定義を、2組の記法で正しく記述する。

## 背景・前提

- `parts/005_exp(X)Yexp(-X)=exp(ad(X))(Y)の証明/004_definition_一般線型群GL(n,CC)とその群構造.typ:8` — 現在 `TODO: これを2組でちゃんと書く`

## スコープ

同ファイルの定義を2組の記法で書き直す。

## 作業内容

### 1. GL(n,CC) の定義

- 集合としての定義: {A ∈ M(n,CC) | det(A) ≠ 0}
- 群構造: 行列の積を演算とする群

### 2. 2組の記法での記述

現在の TODO コメントに従い、2通りの記法で整理する。

## 対象ファイル

- `parts/005_exp(X)Yexp(-X)=exp(ad(X))(Y)の証明/004_definition_一般線型群GL(n,CC)とその群構造.typ`

## 完了条件

- [ ] GL(n,CC) の定義が2組の記法で記述されている
- [ ] 群構造（演算・単位元・逆元）が明示されている
- [ ] `typst compile main.typ` がエラーなしで通過する
