# γ\_{θ\_μ} の具体的な値の計算

## 概要

γ\_{θ\_μ} の具体的な値を求める。固有ベクトルの再チェックを含む。

## 背景・前提

- `main.typ:186-207` — メモに記載。固有ベクトルの検証が必要。SageMath による数値計算コードあり
- `parts/008_T_V1_hatZとhatZ_hatYの関係/026_claim_A_thetaの対角化_固有値と固有ベクトル.typ` — 固有値・固有ベクトルの計算
- `parts/008_T_V1_hatZとhatZ_hatYの関係/027_claim_A_thetaの対角化_P_muとD_mu.typ` — P, D の定義
- 本スコープの 010 (A(θ\_μ) 単位行列の場合) が前提

## スコープ

γ\_{θ\_μ} を A(θ\_μ) の固有値から具体的に計算し、exp(γ) の表式を得る。

## 作業内容

### 1. 固有ベクトルの再チェック

main.typ のメモにある通り、固有ベクトルが正しいか再検証する。期待される形:
v = c (±i√(γ₂(θ\_μ)γ₂(-θ\_μ)), iγ₂(-θ\_μ))^T

### 2. γ\_{θ\_μ} の導出

固有値 exp(±γ) と A(θ\_μ) の固有値の対応から γ を求める。

### 3. SageMath による数値検証

main.typ 内の SageMath コードを使い、結果を数値的に検証する。

## 対象ファイル

- `parts/008_T_V1_hatZとhatZ_hatYの関係/026_claim_A_thetaの対角化_固有値と固有ベクトル.typ`
- 新規ファイルが必要な場合は `parts/008_T_V1_hatZとhatZ_hatYの関係/` に追加

## 完了条件

- [ ] γ\_{θ\_μ} の具体的な値が計算されている
- [ ] 固有ベクトルが正しいことが検証されている
- [ ] SageMath で数値的に確認されている
- [ ] ステートメントと証明が整合している
- [ ] `typst compile main.typ` がエラーなしで通過する
