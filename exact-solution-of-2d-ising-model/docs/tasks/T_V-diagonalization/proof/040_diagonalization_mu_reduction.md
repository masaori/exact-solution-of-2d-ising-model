# 対角化の各μ帰着の正当化

## 概要

「転送行列の対角化は、各μごとに 4×4 行列 A(θ\_μ) を対角化する問題に帰着される」ことを正当化する。

## 背景・前提

- `main.typ:261-272` — メモに記載
- ホロノミック量子場 付録B の記述: 「したがって転送行列の対角化は,各μごとに4行4列の行列A(θ\_μ)を対角化する問題に帰着された。」
- 本スコープの 030 (T\_V = T\_{V'}) が前提
- `parts/008_T_V1_hatZとhatZ_hatYの関係/026_claim_A_thetaの対角化_固有値と固有ベクトル.typ` — A(θ) の固有値済み

## スコープ

V' の固有値が各 A(θ\_μ) の固有値の組み合わせで表されることを示し、V の固有値（命題2.4）との対応を確認する。

## 作業内容

### 1. V' の固有値の分解

V' = exp(-Σ\_μ γ\_μ (ψ\_μ† ψ\_μ - 1/2)) の各 μ 成分が独立に作用することから、固有値が積に分解されることを示す。

### 2. V の固有値（命題2.4）との対応

本の命題2.4で与えられている V の固有値と、上記の分解が一致することを確認する。

### 3. V の固有ベクトルの構成（可能であれば）

A(θ) の固有ベクトルの組み合わせから V の固有ベクトルを構成する。

## 対象ファイル

- 新規ファイルを `parts/008_T_V1_hatZとhatZ_hatYの関係/` に作成する可能性あり

## 完了条件

- [ ] 各μへの帰着が正当化されている
- [ ] V の固有値が A(θ\_μ) の固有値から導かれることが示されている
- [ ] ステートメントと証明が整合している
- [ ] `typst compile main.typ` がエラーなしで通過する
