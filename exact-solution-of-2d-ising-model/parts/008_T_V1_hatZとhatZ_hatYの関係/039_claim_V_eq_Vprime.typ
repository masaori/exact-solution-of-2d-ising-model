#import "../../theorem.typ": theorem, claim, proof, definition, remark, note, theorem_rules
#claim([$V = c V'$ (定数倍を除いて一致)])[
  ある$c in CC^(times)$が存在して、

  $
    V = c dot.op V'
  $

  #proof[TODO: $T$の単射性（クリフォード群の性質）を用いる]
]
